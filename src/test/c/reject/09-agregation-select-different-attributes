{
  "SELECT": {
      ">": ["precio", 10],
    "input": {
      "AGGREGATION": {
        "group_by": ["producto"],
        "aggregations": [{ "SUM": "monto" }],
        "input": { "TABLE": "Ventas" }
      }
    }
  }
}
